本目录包含编写 Python MCP 服务器的模板文件。`minimal-server.py` 是最简可用模板，直接复制后修改即可。

**使用方式：**
```bash
cp ~/.hermes/skills/mcp/mcp-server-python/templates/minimal-server.py ./my-server.py
# 修改 my-server.py 中的 tool 定义
# 然后在 config.yaml 中注册
```
